using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Circulo : MonoBehaviour
{

    static Color[] cores = new Color[3];

    void Start()
    {
        cores[0] = Color.blue;
        cores[1] = Color.green;
        cores[2] = Color.red;
    }

    // Update is called once per frame
    void Update()
    {
        
    }

    /*
    IEnumerator Roda () {

    }
    */
}
